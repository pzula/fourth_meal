<div id="categories">
	<h2>Categories</h2>
	<ul>
	  <% @categories.each do |category| %>
	  <li id="category_<%= category.id %>">
    <p><%= category.name %> <%= link_to "Edit", edit_category_path(category.id)%></p>
	  </li>
	  <% end %>
	</ul>
</div>
