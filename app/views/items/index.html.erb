<html>
  <head>
    <title>Items</title>
  </head>
  <body>
    <% @items.each do |item| %>
      <h1><%= item.title %></h1>
      <p><%= item.description %></p>
      <p><%= item.price %></p>
    <% end %>
  </body>
</html>
